services:
  vector:
    image: timberio/vector:latest-debian
    user: "568:568"
    group_add:
      - "999"
    volumes:
      - ./vector/vector.yaml:/etc/vector/vector.yaml:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./data:/data
    ports:
      - "1514:1514/udp"
    restart: unless-stopped

  sniper:
    image: dungfu/sniper-agent-logging:latest
    user: "568:568"
    build: .
    volumes:
      - ./data:/data
    env_file: .env
    depends_on:
      - vector
    restart: unless-stopped
