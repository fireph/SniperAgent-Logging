[project]
name = "sniper-agent-logging"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=2.0",
    "openai>=1.0",
    "httpx>=0.27",
]

[project.scripts]
sniper = "sniper.main:main"
