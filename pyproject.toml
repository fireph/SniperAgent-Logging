[project]
name = "sniper-agent-logging"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastmcp",
    "openai",
    "httpx",
    "pytimeparse",
]

[project.scripts]
sniper = "sniper.main:main"
